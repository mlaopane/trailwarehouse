{# TrailWarehouse > AppBundle > Resources > views > Shop > index.html.twig #}

{% extends "TrailWarehouseAppBundle::layout.html.twig" %}

{% block title %}
  {{ parent() }} : Boutique
{% endblock %}

{% block breadcrumb %}
  <ol class="breadcrumb">
    <li class="breadcrumb-item">
      <a href="{{ path('home') }}"><span class="glyphicon glyphicon-home"></span></a>
    </li>
    <li class="breadcrumb-item active">
      <a href="{{ path('shop') }}">Boutique</a>
    </li>
  </ol>
{% endblock %}

{% block page_main %}
  <main>

    <!-- # News -->
    <div id="news">

      <!-- # News Carousel -->
      <div id="news-carousel" class="carousel slide" data-ride="carousel">

        <!-- # Indicators -->
        <ol class="carousel-indicators">
          {% for id_diapo in 1..4 %}
          <li class="{{ loop.index0 == 0 ? 'active' : '' }}"
            data-target="#news-carousel" data-slide-to="{{ loop.index0 }}"></li>
          {% endfor %}
        </ol>
        <!-- Indicators # -->

        <!-- # Content -->
        <div class="carousel-inner" role="listbox">
          {% for id_diapo in 1..4 %}
          <div class="carousel-item {{ loop.index0 == 0 ? 'active' : '' }}">
            <img class="" src="{{ asset('images/diapo_' ~ id_diapo ~ '.jpg') }}" alt="Slide {{ loop.index }}">
            <div class="carousel-caption d-none d-md-block">
              <h3 class="caption-title">{{ 'News n°' ~ loop.index }}</h3>
              <p class="caption-content">
                Lorem ipsum dolor sit amet, consectetur adipisicing elit.<br>
                Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
              </p>
            </div>
          </div>
          {% endfor %}
        </div>
        <!-- Content # -->

        <!-- # Prev Button -->
        <a class="carousel-control-prev" href="#news-carousel" role="button" data-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <!-- Prev Button # -->

        <!-- # Next Button -->
        <a class="carousel-control-next" href="#news-carousel" role="button" data-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
        <!-- Next Button # -->

      </div>
      <!-- Carousel # -->

    </div>
    <!-- News Diaporama # -->

    <!-- # Sélections du moment -->
    <div id="selections" class="ness-xs-12">

      <!-- # Sélection -->
      <div id="chaussures" class="selection">
        <h2>Sélection Chaussures</h2>
        <div class="ness-diaporama"></div>

      </div>
      <!-- Sélection # -->

    </div>
    <!-- Sélections du moment # -->

    <!-- # Brands -->
    <div id="brands" class="container">
      <div class="row justify-content-around">
        <div class="brand col-6 col-sm-4 col-md-3 col-lg-2">
          <img src="" alt="" class="round">
        </div>
      </div>
    </div>
    <!-- Brands # -->

  </main>
{% endblock %}
